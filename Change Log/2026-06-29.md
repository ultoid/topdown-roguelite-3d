- Rework sistem serangan basic & heavy attack senjata `rune`.
  - Serangan basic `rune` digabung dengan sistem combo `long_sword` di `player_combat.gd` & `player.gd`.
  - Serangan ke-3 `rune` memanggil proyektil membesar dan damage 2x lipat.
  - Pergerakan lunge/slide maju ke depan ditiadakan khusus untuk basic attack `rune` agar terasa seperti penyihir.
  - Heavy attack `rune` (Charge Attack) diubah menjadi fungsi _Mana Burst_: Menciptakan proyektil silinder radius 3 meter yang memukul mundur musuh tepat sejauh 10 meter.
  - Heavy attack `rune` dikonfigurasi ulang agar menggunakan 20 MP, bukan lagi 30 EP.
  - Damage dari seluruh serangan `rune` telah di-relink ke `magic_attack` (scaling dengan INT), memisahkannya dari `physical_attack`.
- Bugfix:
  - Mengubah scaling Attack Speed di `player_stats.gd` dari AGI menjadi DEX sesuai permintaan.
  - Memperbaiki bug _animation abort_ (animasi terpotong) saat spam klik kiri dengan menambahkan _buffer_ 0.05 detik di _timer_ _cooldown_ _attack_ sehingga `Call Method Track` selalu sempat tereksekusi.
  - Memperbaiki variabel cek typo `player.knockback_velocity` di logika musuh menjadi `knockback_velocity` murni, sehingga fungsi pentalan 10 meter bisa berjalan lancar.
  - Memperbaiki peringatan `!is_inside_tree()` di Godot pada spawn proyektil `rune` (baris posisi rotasi dan `add_child` ditukar agar proyektil sudah legal masuk ke Tree sebelum digeser posisinya).
