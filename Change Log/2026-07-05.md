## 5 Juli 2026
- Mengimplementasikan sistem **Maskable Animation** menggunakan `Blend2` node pada `AnimationTree`.
- Membagi animasi menjadi dua bagian:
  - `UpperStateMachine`: Mengatur pose (stance) senjata di tubuh bagian atas.
  - `BaseStateMachine`: Mengatur gerakan kaki dan tubuh bagian bawah (Locomotion: Idle, Walk, Run).
- Menambahkan logika **Full-Body Attack**: Saat karakter menyerang (Attack, HeavyAttack) atau menggunakan skill/dash, sistem otomatis mematikan mask (`blend_amount` = 0) dan memberikan 100% kontrol pada `BaseStateMachine`.
- Menambahkan **Auto-Fallback System**: Memudahkan penambahan senjata baru dengan langsung menggunakan fungsi `.start()` tanpa perlu membuat panah (transitions) antar senjata di `UpperStateMachine`.
- Memperbaiki sistem pembacaan durasi animasi (`_get_state_length`) di dalam `BlendTree` agar kecepatan animasi ayunan pedang/senjata berkalibrasi kembali dengan sempurna.
- Mulai mengerjakan dan mengatur animasi untuk *Apprentice* dengan senjata *Staff*.
- Memecahkan *edge-case* retargeting FBX *animation-only* dari Unity Asset Store (menghindari kerusakan sumbu rotasi dengan mengonfigurasi *Bone Map* dan *Rest Fixer* / *Load Pose* secara presisi).
