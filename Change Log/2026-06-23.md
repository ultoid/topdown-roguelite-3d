### 23 Juni 2026
- **Refactoring player.gd (Pemisahan Total)**: Mengekstraksi ratusan baris kode tersisa di player.gd menjadi komponen independen (PlayerStats, PlayerLifeSkill, PlayerUIManager, PlayerMovement, dan PlayerInput). Komponen-komponen ini dipasang sebagai *Node* di Scene Tree, membuat file utama player.gd kini berukuran kurang dari 500 baris (hanya berisi deklarasi sinyal, stat, dan inisialisasi dasar), sangat bersih dan mudah dipelihara.
- **Setup Debug Fighter**: Menyediakan skrip autoload untuk debug dengan menggunakan `Fighter` Level 40 berserta *skill* terbuka dan terpasang di quick slot.
- **Pembenahan Animasi**: Mengintegrasikan transisi pergerakan dasar, menambahkan _state_ `HeavyAttack` (`Charge Attack`), serta memperbaiki _bug_ properti `max_spin_time` pada animasi `Cyclone Sweep`.
