### 23 Juni 2026
- **Refactoring player.gd (Pemisahan Total)**: Mengekstraksi ratusan baris kode tersisa di player.gd menjadi komponen independen (PlayerStats, PlayerLifeSkill, PlayerUIManager, PlayerMovement, dan PlayerInput). Komponen-komponen ini dipasang sebagai *Node* di Scene Tree, membuat file utama player.gd kini berukuran kurang dari 500 baris (hanya berisi deklarasi sinyal, stat, dan inisialisasi dasar), sangat bersih dan mudah dipelihara.
- **Setup Debug Fighter**: Menyediakan skrip autoload untuk debug dengan menggunakan `Fighter` Level 40 berserta *skill* terbuka dan terpasang di quick slot.
- **Pembenahan Animasi**: Mengintegrasikan transisi pergerakan dasar, menambahkan _state_ `HeavyAttack` (`Charge Attack`), serta memperbaiki _bug_ properti `max_spin_time` pada animasi `Cyclone Sweep`.
- **Pembersihan Aset**: Menghapus seluruh aset gambar dan *tileset* 2D usang dari versi lama yang sudah tidak lagi digunakan dalam ruang proyek 3D.
- **Sistem Animasi Dinamis (Decoupling)**: Merombak sistem *AnimationTree* pada karakter agar *state* pergerakan dan *combat* merujuk pada format penamaan dinamis (`[Tipe_Senjata]_[Aksi]`), serta durasi animasi *skill* diukur secara langsung dari `.glb`, menghindari *hardcode* dalam `player_combat.gd`.
- **Pembaruan Tutorial Dev**: Mengubah semua istilah 2D menjadi referensi sistem 3D pada `TUTORIAL_DEV.md`, sekaligus menambahkan bab khusus untuk pedoman penambahan *state* animasi dinamis senjata.
- **Integrasi Animasi Senjata (Long Sword)**: Menetapkan alur kerja standar baru dengan menggunakan format `.fbx` dari Blender alih-alih `.glb`. Model pedang dipisahkan dari animasi karakter dan ditambahkan secara dinamis ke dalam genggaman melalui `BoneAttachment3D` di Godot. Animasi diekstrak dan didaftarkan mandiri ke `AnimationPlayer` dengan pembenahan path *track* animasi agar selaras dengan hierarki adegan yang ada.
